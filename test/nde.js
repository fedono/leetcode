/*function test(a, b) {
    console.log(b)
    return {
        test: function(c){
            return test(c, a);
        }
    };
}

var retC = test('good').test('bad');
retC.test('good');
retC.test('good');
retC.test('good');
retC.test('good');
retC.test('good');*/
